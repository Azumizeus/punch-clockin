node_modules
dist
dist-ssr
*.local
*.local.ts
.DS_Store

# archives de travail (ne pas versionner)
*.zip

# capturées et logs de debug
*.png.tmp
build-apk.log

# dossier de travail Freebuff
.freebuff/

# debris d'un filter-branch avorte (montage reseau, non supprimable, inoffensif)
.git-rewrite.bak/
.git-junk-refs/

# debris de controle d'appareil (dumps uiautomator + captures adb, regenerables)
t_*.xml
ui_dump.xml
ui_p.xml
wallet_full.xml
ui_now.png
scan_target.py

# artefacts de build lourds : captures QA et APKs signes (releases/*)
# (/* et pas / : sinon les ! ne peuvent pas re-inclure la video)
punch-native/_shots/
punch-native/releases/*

# la video demo est trackee (attachee aux GitHub Releases), + ses sous-titres
!punch-native/releases/punch-clockin-demo.mp4
!punch-native/releases/punch-clockin-demo.srt
