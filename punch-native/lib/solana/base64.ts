// Native (iOS/Android) : quick-base64, la base64 JSI rapide utilisée par le
// Seed Vault. Ce module n'est résolu que hors web (voir base64.web.ts).
export { toByteArray } from "react-native-quick-base64";
